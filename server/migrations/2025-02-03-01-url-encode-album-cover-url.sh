#!/bin/bash
# can sql url encode a string? that would be cool.
# otherwise, import vitals, connect to db, update each row automatically.
#
# SELECT TODO.url.encode(album_cover_url) as url
# FROM albums
#
# UPDATE albums
# SET album_cover_url = TODO.url.encode(album_cover_url)
